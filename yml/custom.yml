custom:
    tableName: "contacts-${self:provider.stage}"
    export-env: # serverless-export-env config
        filename: .awsenv
        overwrite: true
    prune:
        automatic: true
        number: 1