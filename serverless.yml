service: serverless-crud

frameworkVersion: "3"

provider:
  name: aws
  runtime: nodejs18.x

functions:
  hello:
    handler: handler.hello

plugins:
  - serverless-offline
  - serverless-webpack
  - serverless-iam-roles-per-function